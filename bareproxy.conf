# BareProxy development configuration

max_connections = 128
client_idle_timeout_seconds = 30
upstream_timeout_seconds = 10

# TLS identities are independent from HTTP routes.
# tls_identity = certs/localhost.pem | certs/localhost-key.pem

localhost -> 127.0.0.1:3000